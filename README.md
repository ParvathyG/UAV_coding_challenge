### UAV_coding_challenge

Design and implement a dashboard to visualize data from the file asteria_data.json.

#### Installation

Install the dependencies and devDependencies

```sh
$ sudo npm install
$ bower update
```

#### Run

```sh
$ grunt
```
Serve the dist folder. Eg:

```sh
$ cd dist/
$ python -m SimpleHTTPServer
```